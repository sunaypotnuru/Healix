# ═════════════════════════════════════════════════════════════════════════════
# NetraAI - Complete Environment Configuration
# ═════════════════════════════════════════════════════════════════════════════
# Copy this file to .env and replace all placeholders with real values
# For local Docker Compose: Use URLs like http://service-name:port
# For production: Use real domain names and secure URLs
# ═════════════════════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────
# 1. SUPABASE - Database & Authentication
# ─────────────────────────────────────────────────────────────
# Backend Service Role Key (for API operations)
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_SERVICE_KEY="your-service-role-key-here"

# Frontend Anonymous Key (for browser clients)
VITE_SUPABASE_URL="https://your-project.supabase.co"
VITE_SUPABASE_ANON_KEY="your-anon-key-here"

# ─────────────────────────────────────────────────────────────
# 2. LIVEKIT - Video Consultations & Real-time Communication
# ─────────────────────────────────────────────────────────────
# Get these from https://cloud.livekit.io (or self-hosted instance)
LIVEKIT_API_KEY="your-livekit-api-key"
LIVEKIT_API_SECRET="your-livekit-api-secret"
# For Docker Compose local dev use: wss://livekit:7880
# For production use: wss://your-domain.livekit.cloud
LIVEKIT_URL="wss://your-project.livekit.cloud"

# ─────────────────────────────────────────────────────────────
# 3. MICROSERVICES URLS
# ─────────────────────────────────────────────────────────────
# AI Anemia Detection Service
ANEMIA_API_URL="http://anemia-service:8001"

# LibreTranslate (Self-hosted multilingual translation)
# Supports: en, es, fr, hi, ta, te, kn, ml, pa, bn, gu, mr, or
LIBRETRANSLATE_URL="http://libretranslate:5000"

# ─────────────────────────────────────────────────────────────
# 4. FRONTEND CONFIGURATION (Vite)
# ─────────────────────────────────────────────────────────────
# Backend API Base URL (for frontend)
# Local: http://localhost:8000
# Docker: http://backend:8000
# Production: https://api.your-domain.com
VITE_API_URL="http://localhost:8000"

# ─────────────────────────────────────────────────────────────
# 5. GOOGLE CLOUD (Optional - for Translation)
# ─────────────────────────────────────────────────────────────
# Path to GCP service account JSON (if using Google Translation API)
GOOGLE_APPLICATION_CREDENTIALS="/run/secrets/gcp-key.json"

# ─────────────────────────────────────────────────────────────
# 6. DEVELOPMENT & DEBUGGING
# ─────────────────────────────────────────────────────────────
# Bypass Supabase authentication (for UI testing without credentials)
# WARNING: Only use in development! Never enable in production
BYPASS_AUTH=false
VITE_BYPASS_AUTH=false

# ─────────────────────────────────────────────────────────────
# 7. BACKEND API CONFIGURATION
# ─────────────────────────────────────────────────────────────
# API Version prefix
API_V1_STR="/api/v1"

# Environment
ENVIRONMENT="development"  # or "production"

# ═════════════════════════════════════════════════════════════════════════════
# SETUP INSTRUCTIONS
# ═════════════════════════════════════════════════════════════════════════════
#
# 1. Create a Supabase project: https://supabase.com
#    - Get Service Role Key from Project Settings → API
#    - Get Anon Key from Project Settings → API
#    - Create database tables by running: supabase_schema.sql
#
# 2. Create a LiveKit project: https://cloud.livekit.io
#    - Generate API Key and Secret from dashboard
#    - Note the URL (e.g., wss://project.livekit.cloud)
#
# 3. For Google Cloud Translation (optional):
#    - Create a service account JSON and reference it
#
# 4. Run the full stack:
#    $ docker-compose up --build
#
# ═════════════════════════════════════════════════════════════════════════════
